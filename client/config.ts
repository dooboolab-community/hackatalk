import Constants from 'expo-constants';

const extra = Constants.manifest.extra;

export const GRAPHQL_URL = extra.GRAPHQL_URL;
export const ROOT_URL = extra.ROOT_URL;
export const facebookAppId = extra.facebookAppId;
export const facebookSecret = extra.facebookSecret;
export const googleClientId = extra.googleClientId;
export const googleSecret = extra.googleSecret;
